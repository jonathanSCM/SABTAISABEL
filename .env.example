DATABASE_URL=postgres://usuario:password@localhost:5432/santa_isabel
SESSION_SECRET=cambia-esto-por-algo-largo-y-aleatorio
PORT=4000
NODE_ENV=development

# Opcional: solo si esta app corre SIN un proxy (Coolify/Traefik/nginx) delante
# y necesita hablar HTTPS directamente. Generalos con: npm run cert:selfsigned
# SSL_KEY_PATH=/ruta/a/certs/key.pem
# SSL_CERT_PATH=/ruta/a/certs/cert.pem
